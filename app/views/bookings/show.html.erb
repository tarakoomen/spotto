<div class="main-container">
  <h1>Your booking details</h1>
  <div class="container">
    <%= link_to "Back to my bookings", bookings_path %>
    <div class="slider-container">
      <div class="slider-group">
          <div class="card">
            <div class="content-img">
              <img src="https://cdn.pixabay.com/photo/2014/07/31/00/30/vw-beetle-405876_1280.jpg" class="card-img-top" alt="...">
            </div>
              <div class="card-body">
                <h5 class="card-title">
                  <%= @booking.space.name %>
                </h5>
                <h6 class="card-text">
                  <%= @booking.space.location %>
                </h6>
                 <h6 class="card-text">
                  <%= @booking.space.description %>
                </h6>
                 <h6 class="card-text">
                  $<%= @booking.space.price %>
                </h6>
                <h6 class="card-text">
                  Start date:
                  <%= @booking.start %>
                </h6>
                <h6 class="card-text">
                  End date:
                  <%= @booking.end %>
                </h6>
                  </div>
                    <div class="map-container">
                    <%= image_tag("generic_map.png", class: "map-image", style: "border-radius: 50%; width: 120px; height: `120`px;") %>
                    </div>
                    <%# <div>
                      <h5>Add your review</h5>
                      <%= simple_form_for [@booking, @review] do |f| %>
                      <%# <%= f.input :content, placeholder: "Add your review here..." %>
                      <%# <%= f.input :rating, as: :select, collection: (1..5), required: true, prompt: "What's your rating", input_html: { data: { controller: "star-rating" } } %>
                      <%# <%= f.submit class:"btn btn-flat" %>
                      <%# <% end %>
                    <%# </div> %>
            <div>
              <p><%= link_to "Edit this booking", edit_booking_path(@booking)%></p>
              <p>
              <%= link_to "Cancel this booking", @booking.space,
                  data: { turbo_method: :delete, turbo_confirm: "Are you sure?" },
                  class: "text-danger"
                %>
              </p>
              <p><%= link_to "Write a review", new_booking_review_path(@booking) %></p>
            </div>
              </div>
          </div>
      </div>
    </div>
  </div>
</div>
